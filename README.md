# Makemore

A character-level language model built with PyTorch.
Trained to generate human-like names by learning character patterns.

## What it does
- Learns patterns from character sequences
- Generates new names/words that sound realistic
- Implements bigram and neural network approaches

## Built with
Python, PyTorch.

## Status
Work in progress — actively improving the model
